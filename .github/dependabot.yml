version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    # Batch action bumps into a single PR instead of one per action.
    groups:
      github-actions:
        patterns:
          - "*"
